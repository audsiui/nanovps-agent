{
  "name": "nanovps-agent",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run src/index.ts",
    "clean": "rm -rf dist",
    "build": "bun run build:linux-x64 && bun run build:linux-arm64",
    "build:linux-x64": "bun build --compile --target=bun-linux-x64 --outfile=dist/nanovps-agent-linux-amd64 src/index.ts",
    "build:linux-arm64": "bun build --compile --target=bun-linux-arm64 --outfile=dist/nanovps-agent-linux-arm64 src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "systeminformation": "^5.30.6",
    "winston": "^3.19.0",
    "winston-daily-rotate-file": "^5.0.0"
  }
}